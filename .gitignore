# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# Env / secrets
.env
.env.local

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Future frontend (Next.js) / node
node_modules/
.next/
out/

# Local scan/database artifacts (Milestone 2+)
*.db
*.sqlite3

# Milestone 3: sandbox scratch data - regenerated by sandbox.reset_sandbox_dir()
# on every run, never meant to be committed (and may contain that run's canary
# secret / leaked-file evidence, which is test fixture noise, not source).
runtime/sandbox_data/
